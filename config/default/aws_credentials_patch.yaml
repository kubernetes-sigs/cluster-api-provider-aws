apiVersion: v1
kind: Secret
metadata:
  name: manager-bootstrap-credentials
  namespace: system
data:
  # Change the value of CREDENTIALS to the actual credential values for the manager
  credentials: CREDENTIALS
