apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: aws-provider-controller-manager
  namespace: aws-provider-system
spec:
  template:
    spec:
      containers:
      - image: ${MANAGER_IMAGE}
        name: manager
