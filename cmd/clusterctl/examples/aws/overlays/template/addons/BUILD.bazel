load("@bazel_tools//tools/build_defs/pkg:pkg.bzl", "pkg_tar")

pkg_tar(
    name = "addons",
    srcs = [
        "kustomization.yaml",
    ],
    package_dir = "addons",
    visibility = ["//visibility:public"],
)
