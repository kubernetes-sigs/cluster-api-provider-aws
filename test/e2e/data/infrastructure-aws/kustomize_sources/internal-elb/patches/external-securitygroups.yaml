apiVersion: infrastructure.cluster.x-k8s.io/v1beta1
kind: AWSCluster
metadata:
  name: "${CLUSTER_NAME}"
spec:
  network:
    securityGroupOverrides:
      controlplane: "${WL_SG_ID}"
      apiserver-lb: "${WL_SG_ID}"
      node: "${WL_SG_ID}"
      lb: "${WL_SG_ID}"
      bastion: ${WL_SG_ID}

