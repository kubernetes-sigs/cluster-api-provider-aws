{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "delete-artifacts",
            "linux": {
                "command": "rm -rf ${workspaceFolder}/_artifacts"
            },
            "osx": {
                "command": "rm -rf ${workspaceFolder}/_artifacts"
            },
        }
    ]
}