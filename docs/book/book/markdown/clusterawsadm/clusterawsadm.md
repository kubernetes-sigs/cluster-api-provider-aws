## clusterawsadm

Kubernetes Cluster API Provider AWS Management Utility

### Synopsis

clusterawsadm provides helpers for bootstrapping Kubernetes Cluster API Provider AWS. Use clusterawsadm to view required AWS Identity and Access Management (IAM) policies as JSON docs, or create IAM roles and instance profiles automatically using AWS CloudFormation.

 clusterawsadm additionally helps provide credentials for use with clusterctl.

```
clusterawsadm [flags]
```

### Examples

```
  # Create AWS Identity and Access Management (IAM) roles for use with
  # Kubernetes Cluster API Provider AWS.
  clusterawsadm bootstrap iam create-cloudformation-stack
  
  # Encode credentials for use with clusterctl init
  export AWS_B64ENCODED_CREDENTIALS=$(clusterawsadm bootstrap credentials encode-as-profile)
  clusterctl init --infrastructure aws
```

### Options

```
  -h, --help    help for clusterawsadm
  -v, --v int   Set the log level verbosity. (default 2)
```

### SEE ALSO

* [clusterawsadm ami](clusterawsadm_ami.md)	 - AMI commands
* [clusterawsadm bootstrap](clusterawsadm_bootstrap.md)	 - bootstrap commands
* [clusterawsadm controller](clusterawsadm_controller.md)	 - controller commands
* [clusterawsadm eks](clusterawsadm_eks.md)	 - Commands related to EKS
* [clusterawsadm gc](clusterawsadm_gc.md)	 - Commands related to garbage collecting external resources of clusters
* [clusterawsadm resource](clusterawsadm_resource.md)	 - Commands related to AWS resources
* [clusterawsadm version](clusterawsadm_version.md)	 - Print version of clusterawsadm

###### Auto generated by spf13/cobra on 14-Jan-2026
